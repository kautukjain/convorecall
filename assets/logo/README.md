# assets/logo

The live product lockup is **not here** — it is `apps/web/public/logo/`, because the web app has to
serve it and a README image should not be a second copy that drifts from the one users see.

The app icon and favicon live in `assets/icons/`.

`mark.svg` is the earlier hand-drawn mark (waveform plus tick), superseded by the supplied lockup.
Nothing references it; kept alongside `assets/icons/legacy-mark.svg` for provenance only.
